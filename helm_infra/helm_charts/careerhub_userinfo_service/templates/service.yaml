apiVersion: v1
kind: Service
metadata:
  name: {{.Values.restapi.name}}
spec:
  type: ClusterIP
  selector:
    app: {{.Values.name}}
  ports:
    - protocol: TCP
      port: {{.Values.restapi.grpcPort}}
      targetPort: {{.Values.restapi.grpcPort}}
