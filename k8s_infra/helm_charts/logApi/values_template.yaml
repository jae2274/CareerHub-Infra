name: ${charts.log_api.name}
image: ${charts.log_api.image}
tag: ${charts.log_api.tag}
apiPort: ${charts.log_api.api_port}
mongoUri: #known after the deployment
dbName: ${charts.log_api.db_name}
dbUsername: #known after the deployment
dbPassword: #known after the deployment
